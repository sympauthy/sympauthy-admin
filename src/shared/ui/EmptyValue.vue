<template>
  <!--
    What a record does not carry, wherever it would otherwise be read. It stays in the flow of the
    sentence it sits in rather than disappearing, so a field that is empty and a field that has gone
    missing cannot be mistaken for one another — and it drops back to the body face, since a dash
    rendered in the monospace of the value beside it reads as part of that value.
  -->
  <span class="font-sans text-gray-300">&mdash;</span>
</template>
