/**
 * The element a page's actions are drawn into. Named here rather than written on both sides, since
 * the bar that renders it and the pages that fill it never meet.
 */
export const PAGE_ACTIONS_OUTLET = 'page-actions'
